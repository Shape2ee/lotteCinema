/* 
  while 반복문: 조건이 참인 동안 반복

  let 식별자 = 초기값;
  while(조건) { <- 종료될 수 있는 조건 필요
    조건이 만족될 때 까지의 실행문;
    증감식; <- 이 값이 없으면 초기값으로 무한 반복
  }
 */

  for(let i = 1; i <= 10; i++){
    console.log(`*`);
  }